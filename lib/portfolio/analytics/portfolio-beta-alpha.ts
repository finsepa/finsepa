/**
 * Alpha / Jensen metrics — not shown in current Key Stats UI.
 * Phase 4: BLOCKED (no UI surface). Do not wire without a product label.
 */

export const ALPHA_STATUS = "BLOCKED" as const;
export const ALPHA_REASON = "Not represented in current Manual Portfolio Key Stats UI.";
