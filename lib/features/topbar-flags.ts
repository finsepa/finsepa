/**
 * Temporary UI flags — set to `true` to restore notifications, quick-add, and portfolio pill.
 * Underlying markup stays in `topbar.tsx` behind conditionals.
 */
export const TOPBAR_SHOW_NOTIFICATIONS = false;
export const TOPBAR_SHOW_QUICK_ADD = false;
export const TOPBAR_SHOW_PORTFOLIO_BLOCK = false;
