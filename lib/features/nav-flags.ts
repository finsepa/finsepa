/**
 * Temporary navigation flags — flip to `true` to re-expose News, Earnings, Economy, Macro in the sidebar.
 * Routes and APIs remain deployed; only active nav + Link prefetch are gated.
 */
export const NAV_NEWS_ENABLED = false;
export const NAV_EARNINGS_ENABLED = true;
export const NAV_ECONOMY_ENABLED = true;
export const NAV_MACRO_ENABLED = true;
