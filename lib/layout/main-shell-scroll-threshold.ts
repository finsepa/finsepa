/** Scroll distance on shell `<main>` before the “back to top” FAB appears. */
export const MAIN_SHELL_SCROLL_THRESHOLD_PX = 150;

/**
 * Mobile `<main>` `translateY` when the top bar is fully visible. Must match `--mobile-topbar-push` in `app/globals.css`.
 * Also used as the scroll distance (px) over which the mobile top bar fully collapses off-screen.
 */
export const MOBILE_SHELL_TOPBAR_PUSH_PX = 68;
