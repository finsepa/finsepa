/** Scroll distance on shell `<main>` before “scroll down” chrome (FAB, mobile top bar) activates. */
export const MAIN_SHELL_SCROLL_THRESHOLD_PX = 150;

/**
 * Mobile `<main>` `translateY` when the top bar is visible. Must match `--mobile-topbar-push` in `app/globals.css`.
 */
export const MOBILE_SHELL_TOPBAR_PUSH_PX = 68;
